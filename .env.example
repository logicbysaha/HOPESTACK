# Database Connection URI (MongoDB Atlas)
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/hopestack

# Security Secret for JWT
JWT_SECRET=your_super_secure_random_key_here

# Optional: Server Port
PORT=5000

# Node Environment (production/development)
NODE_ENV=production
